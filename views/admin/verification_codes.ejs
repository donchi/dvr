<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Verification Codes - Digital Visitor Registry (DVR)</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link rel="stylesheet" href="../css/admin.css">
  <link rel="shortcut icon" href="../images/favicon.png" type="image/png">
</head>
<body>
  <!-- Header -->
  <%- include ('includes/header') %>

  <!-- Side Navigation -->
  <%- include ('includes/sidenav') %>

    <!-- Main Content -->
  <main class="main-content">
    <div class="page-header">
      <h2>Verification Codes</h2>
      <button class="btn" onclick="window.history.back()"><i class="fas fa-arrow-left"></i> Back</button>
    </div>
    <h4 style="background-color: white; padding: 10px; border-radius: 5px; font-weight: 500;">Ensure you make a call to the phone number before viewing the code. Click the eye button to view the code.</h4>
   
    <div class="search-filter-container">
      <div class="search-box">
        <i class="fas fa-search"></i>
        <input type="text" placeholder="Search phone number...">
      </div>
    </div>
    
    <div class="table-container">
      <table class="visitors-table">
        <thead>
          <tr>
            <th>Phone Number</th>
            <th>Verification Code</th>
            <th>Created at</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% verifications?.forEach(array => { %>
           <tr>
            <td><%= array?.phone %></td>
            <td> <span data-row="<%= array?.code %>"><%= array?.code.toString().replace(/./g, '*') %></span> </td>
            <td><%= new Date(array?.timestamp).toLocaleString([], {  month: 'numeric', day: 'numeric', year: 'numeric', hour: 'numeric', minute: '2-digit', hour12: true }) %></td>
            <td><button class="btn" data-row="<%= array?.phone %>"><i class="fas fa-eye-slash"></i></button> </td>
          </tr>
          <% }) %>
          
        </tbody>
      </table>
  
      <!-- Pagination Controls -->
      <div class="pagination">
        <div class="pagination-info">Showing 1-10 of 24 visitors</div>
        <div class="pagination-controls">
          <button class="pagination-btn" disabled>
            <i class="fas fa-chevron-left"></i>
          </button>
          <button class="pagination-btn active">1</button>
          <button class="pagination-btn">2</button>
          <button class="pagination-btn">3</button>
          <button class="pagination-btn">
            <i class="fas fa-chevron-right"></i>
          </button>
        </div>
        <div class="pagination-page-size">
          <span>Rows per page:</span>
          <select>
            <option>10</option>
            <option>25</option>
            <option>50</option>
            <option>100</option>
          </select>
        </div>
      </div>
    </div>
    </div>
  </main>

<script type="module" src="../scripts/admin.js"></script>
  
</body>
</html>